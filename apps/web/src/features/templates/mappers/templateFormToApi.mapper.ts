import type { CreateTemplateFormValues, CreateTemplatePayload } from "../types/template.types";

export function mapTemplateFormToApiPayload(values: CreateTemplateFormValues): CreateTemplatePayload {
  return values;
}
