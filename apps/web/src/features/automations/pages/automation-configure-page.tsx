export function AutomationConfigurePage() {
  return (
    <section
      aria-labelledby="automation-configure-title"
      className="rounded-lg border border-border bg-surface p-5 shadow-sm"
    >
      <h2 id="automation-configure-title" className="text-xl font-semibold text-text">
        Configure automation
      </h2>
      <p className="mt-2 max-w-2xl text-sm text-text-muted">
        Automation configuration placeholder. Real triggers, queues, and WhatsApp send behavior stay out of scope until
        the automation feature plan is approved.
      </p>
    </section>
  );
}
