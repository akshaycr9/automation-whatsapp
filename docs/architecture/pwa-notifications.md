# PWA Notifications

The app should be mobile-first and installable later. Manifest and service worker support will be added during the PWA phase.

Push notifications should focus on inbound customer messages and deep-link to the relevant conversation. System events should not create notification noise.

Notification permission should be requested after clear admin intent, not automatically on first page load.
